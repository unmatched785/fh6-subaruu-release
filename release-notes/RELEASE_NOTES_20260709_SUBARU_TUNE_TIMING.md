# 새 작업차 튜닝 기준 기본 시간 조정 2026-07-09

새 스바루 작업차 튜닝 기준으로 기본 주행 시간과 보정 주행 시간을 조정했습니다.

## 다운로드

- Asset: `subaruu.zip`
- SHA256: `84B901207B5B3C0136F0ABB7C496A23F3722E2953DCA94014FD611E0829B453E`

## 변경

- 스바루 작업차 튜닝 코드를 `171 460 155`로 안내합니다.
- 위 튜닝 기준 FHD 정상 주행 시간 안내를 한 판 약 4분 1초대로 조정했습니다.
- 프로그램 기본 주행 시간을 `265초`에서 `245초`로 조정했습니다.
- 올인원 보정 주행 시간을 `130초`에서 `120초`로 조정했습니다.
- 보정 주행은 약 50포인트를 추가 확보하는 안전마진으로 유지했습니다.
- 수동 스바루런 10회 테스트에서는 989포인트를 확인했습니다.
- 구매, 강화, 삭제, 보유 CR 계산 로직은 이번 릴리즈에서 변경하지 않았습니다.

## 확인

- `dotnet publish SubaruuDirectRun.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`
- ZIP 안쪽 루트 폴더가 `subaruu/` 하나인 것 확인
- ZIP 안에 `subaruu.conf`, `subaruu.conf.bak`, `log` 폴더가 포함되지 않는 것 확인
- ZIP SHA256 계산 완료

## 문제 보고

포인트가 부족하거나 이벤트랩 시간이 비정상적으로 느리면 먼저 작업차 튜닝 코드, 작업차 핵심 마스터리, 프레임 제한을 확인해주세요.
문제가 계속되면 `F2`로 즉시 멈추고, `subaruu.exe` 옆의 `log` 폴더와 `subaruu.conf`를 같이 보내주세요.
`last-run-status.txt`도 `log` 폴더 안에 함께 저장됩니다.

사용은 전적으로 사용자 본인 책임입니다.
