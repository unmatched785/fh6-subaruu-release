# 스바루작 기본값 핫픽스 2026-06-29

첫 실행 기본값을 다시 `스바루작` 기준으로 맞춘 핫픽스입니다.

## 다운로드

- Asset: `subaruu.zip`
- SHA256: `BA44F817239C4A2429043C6ED09A3CC7C858BF8D29FD18AD752C6CD303BCDB49`

## 변경

- `subaruu.default.conf`의 첫 실행 기본값을 `마즈다작` 47/47/46에서 `스바루작` 33/33/32로 변경했습니다.
- 기본 자동차 마스터리 경로를 스바루작 경로로 되돌렸습니다.
- 설정 파일이 없을 때 사용하는 프로그램 내부 기본값도 스바루작 기준으로 통일했습니다.
- `마즈다작`은 카패스 DLC 차량 보유자용 선택 프리셋이라고 README/GUIDE에 명시했습니다.
- 자동차 컬렉션 경로와 자동차 매장/상품권/스토어 오진입 경고를 더 분명하게 정리했습니다.
- `로딩 대기` 기본값은 느린 PC 안전을 위해 30초로 유지했습니다.
  빠른 PC에서는 테스트런으로 확인한 뒤 20초까지 줄일 수 있습니다.

## 유지

- 스바루런 주행 중 `W`와 `↑ 방향키`를 함께 누르는 입력 방식은 그대로 유지됩니다.
- 스바루런 후 오픈월드 복귀 대기에도 `스바루런` 탭의 `로딩 대기` 값을 쓰는 동작은 그대로 유지됩니다.
- 배포 ZIP에는 사용자 설정 파일 `subaruu.conf`가 포함되지 않습니다.

## 주의

기존 폴더에 덮어 풀면 이미 생성된 `subaruu.conf`는 유지됩니다.
이미 마즈다작 값으로 저장해 둔 사용자는 슈퍼휠 탭에서 `스바루작` 프리셋을 누르거나, 새 폴더에 압축을 풀어 첫 실행 기본값을 다시 받으세요.

사용은 전적으로 사용자 본인 책임입니다.
매주 목요일 23:30 전후에는 제조사/차량 위치가 바뀔 수 있으니 반드시 테스트런으로 확인하세요.
