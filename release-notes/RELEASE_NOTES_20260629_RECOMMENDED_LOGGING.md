# 최신 권장 빌드 - 사용 로그 추가 2026-06-29

처음 받는 사람은 이 릴리즈만 받으세요.
이전 2026-06-29 릴리즈들의 수정 사항을 포함한 최신 권장 빌드입니다.

## 다운로드

- Asset: `subaruu.zip`
- SHA256: `3B304972C133D27CE90873C2C7FBED4B1B0AF4804A6840D25CF4D5686C52E223`

## 변경

- 실행 폴더 옆 `log` 폴더에 `session-날짜-시간.log` 사용 로그를 자동 저장합니다.
- 로그에는 앱 시작/종료, 설정 저장, 실행 탭, 주요 설정값, 진행 상태, 중단 이유가 남습니다.
- 이벤트랩 드라이런 리포트와 인식 실패 캡처 경로도 세션 로그에서 찾을 수 있습니다.
- README/GUIDE에 문제 보고 시 `log` 폴더와 `subaruu.conf`를 같이 보내달라는 안내를 추가했습니다.
- README/GUIDE 상단에 `Latest` 릴리즈만 받으라는 안내를 추가했습니다.

## 포함된 이전 수정

- 첫 실행 기본값을 `스바루작` 기준으로 통일했습니다.
- 스바루런 주행 중 `W`와 `↑ 방향키`를 함께 누르는 입력 방식을 유지합니다.
- 스바루런 후 오픈월드 복귀 대기에도 `스바루런` 탭의 `로딩 대기` 값을 씁니다.
- 배포 ZIP에는 사용자 설정 파일 `subaruu.conf`가 포함되지 않습니다.

## 문제 보고

문제가 생기면 `F2`로 즉시 멈추고, `subaruu.exe` 옆의 `log` 폴더와 `subaruu.conf`를 같이 보내주세요.

사용은 전적으로 사용자 본인 책임입니다.
매주 목요일 23:30 전후에는 제조사/차량 위치가 바뀔 수 있으니 반드시 테스트런으로 확인하세요.
